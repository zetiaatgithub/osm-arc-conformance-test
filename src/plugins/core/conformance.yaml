sonobuoy-config:
  driver: Job
  plugin-name: azure-arc-osm-conformance
  result-format: junit
spec:
  image: eduser25/osm-arc-conformance:beta
  imagePullPolicy: Always
  name: plugin
  resources: {}
  volumeMounts:
  - mountPath: /tmp/results
    name: results